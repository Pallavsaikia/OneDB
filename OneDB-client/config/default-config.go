package config

const (
    DEFAULT_USERNAME="root"
    DEFAULT_HOST="127.0.0.1"
    DEFAULT_PORT="3407"
)
func establishConnection(username string, database string, port string, host string,password string) {
	// fmt.Print("Trying to connect to "+host+":"+port+" with user :"+username)
	return
}
